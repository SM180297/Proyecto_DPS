import React from 'react';
import {View, Text,Component,Input, StyleSheet} from 'react-native';
import { Input } from 'react-native-elements'; 
import Icon from 'react-native-vector-icons/FontAwesome';

export default class citas extends Component{
  /*  state={ 
        usuario:'', 
        contrasena:'' 
         
          }
    render(){
        return(
            <View>
                <View>
                    <Text>Iniciar Sesión</Text>
                    
                </View>
                <View>
                    <Text style={styles.label}>Usuario: </Text>
                    <Input style={styles.input} onChangeText={(text) => this.setState({usuario:text}) } rightIcon={<Icon name='user' size={24} color='black'/>}/>
                </View>
                <View>
                    <Text style={styles.label}>Contraseña: </Text>
                    <TextInput placeholder="*****" style={styles.input} onChangeText={ (text) => this.setState({contrasena:text}) } secureTextEntry={true} rightIcon={<Icon name='lock' size={24} color='black'/>}/>
                </View>
            </View>
        );
    }*/
}
export default Cita;

